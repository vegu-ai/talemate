{% if regeneration_context and regeneration_context.direction -%}
{% if regeneration_context.method == 'replace' -%}
The writer has been given specific instructions:

```
{{ regeneration_context.direction }}
```
{%- elif regeneration_context.method == 'edit' and regeneration_context.message -%}
The story writer has written a first draft of the narration:

``` first draft
{{ regeneration_context.message }}
```

{% if original_instructions %}Original instructions: "{{ original_instructions }}"
{% endif -%}
The story writer was given the following editorial instructions: "{{ regeneration_context.direction }}"
{% endif -%}
{% endif -%}