{#-
Extractors (template-defined):
  - response: AnchorExtractor("<ATTRIBUTE>", "</ATTRIBUTE>", fallback_to_full=True)
-#}
{#
  TASK: Character Attribute Generation
  Generates or rewrites a character attribute value.
  Expected output: Short, concise attribute text (no markdown), wrapped in <ATTRIBUTE> tags
#}
{{ action_task }} "{{ context_name }}" attribute for {{ character_name }}. This must be a general description or specific value (depending on the attribute) and not a continuation of the current narrative. Keep it short and concise.

YOU MUST NOT USE MARKDOWN IN YOUR RESPONSE.

{% with output_tag="ATTRIBUTE", output_label="attribute value" %}{% include "contextual-generate/output-format.jinja2" %}{% endwith %}
