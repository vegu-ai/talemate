services:
  talemate:
    extends:
      file: docker-compose.cpu.yml
      service: talemate
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
