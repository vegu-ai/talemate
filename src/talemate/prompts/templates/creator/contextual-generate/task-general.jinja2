{#-
Extractors (template-defined):
  - response: AnchorExtractor("<CONTENT>", "</CONTENT>", fallback_to_full=True)
-#}
{#
  TASK: General Context Generation
  Default fallback for unspecified context types.
  Handles both question format and general content.
#}
{% if context_name.endswith("?") %}
{{ action_task }} answer to the question "{{ context_name }}". This must be a general description and not a continuation of the current narrative.

{% with output_tag="CONTENT", output_label="answer" %}{% include "contextual-generate/output-format.jinja2" %}{% endwith %}
{% else %}
{{ action_task }} new narrative content for {{ context_name }}
{% if not generation_context.style %}Use a simple, easy to read writing format.{% endif %}

{% with output_tag="CONTENT", output_label="content" %}{% include "contextual-generate/output-format.jinja2" %}{% endwith %}
{% endif %}
