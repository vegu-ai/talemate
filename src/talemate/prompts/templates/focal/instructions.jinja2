Use the following syntax to execute your tasks. Each instruction begins with the action and is then followed by one or more arguments. 

All arguments are required.

Arguments are separated using the {{ state.argument_delimiter }} character.

Start each instruction with the {{ state.callback_prefix }} token.
End each instruction with the {{ state.callback_suffix }} token.